<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Call Screening role — not a permission, requested via RoleManager -->
    <!-- android.app.role.CALL_SCREENING is declared in the service below -->

    <!-- NOT requested: READ_CALL_LOG, READ_CONTACTS, RECORD_AUDIO -->

    <application
        android:name=".CallGuardApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallGuard">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Call Screening Service -->
        <service
            android:name=".screening.CallGuardScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Not Spam notification action receiver -->
        <receiver
            android:name=".notification.NotSpamActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.fenn.callguard.action.NOT_SPAM" />
            </intent-filter>
        </receiver>

        <!-- WorkManager initialized manually via CallGuardApp (Configuration.Provider).
             The default auto-init via androidx.startup is intentionally removed to
             prevent double-initialization and crashes. -->

    </application>

</manifest>
