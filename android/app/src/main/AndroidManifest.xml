<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Camera — used only for QR pairing scan (Phase 3 Family Protection) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Call Screening role — not a permission, requested via RoleManager -->
    <!-- android.app.role.CALL_SCREENING is declared in the service below -->

    <!-- READ_CONTACTS — used only for Contacts Only blocking mode (Advanced Blocking Policy).
         Numbers are never uploaded; lookup is on-device only. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Package visibility: required on API 30+ for resolving implicit intents -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
    </queries>

    <application
        android:name=".CallShieldApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallShield">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CallShield.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Call Screening Service -->
        <service
            android:name=".screening.CallShieldScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- Not Spam notification action receiver -->
        <receiver
            android:name=".notification.NotSpamActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.fenn.callshield.action.NOT_SPAM" />
            </intent-filter>
        </receiver>

        <!-- WorkManager initialized manually via CallShieldApp (Configuration.Provider).
             The default auto-init via androidx.startup is intentionally removed to
             prevent double-initialization and crashes. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

    </application>

</manifest>
