# Copy this file to local.properties and fill in your values.
# local.properties is gitignored and never committed.

# Android SDK path (auto-set by Android Studio)
sdk.dir=/Users/YOUR_USERNAME/Library/Android/sdk

# Supabase — get these from your Supabase project dashboard
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# HMAC salt — bundled in binary, NOT a secret, but keep consistent across builds
# Default is used if not set: "callshield-v1-salt-2024"
# HMAC_SALT=callshield-v1-salt-2024

# Promo codes — store ONLY the SHA-256 hash of each raw code, never the raw code itself
# Generate a hash: printf '%s' "YOUR_CODE" | shasum -a 256 | awk '{print $1}'
# Expiry is epoch-milliseconds (Long); omit to use the default baked into build.gradle.kts
# Leave hash empty to disable that promo slot.
# PROMO_CODE_PRO_HASH=<sha256 of your pro promo code>
# PROMO_CODE_PRO_EXPIRY=1790812799000
# PROMO_CODE_FAMILY_HASH=<sha256 of your family promo code>
# PROMO_CODE_FAMILY_EXPIRY=1790812799000
